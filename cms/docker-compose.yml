services:
  db:
    image: mariadb:11
    container_name: lama_wp_db
    restart: unless-stopped
    environment:
      MARIADB_DATABASE: wordpress
      MARIADB_USER: wordpress
      MARIADB_PASSWORD: wordpress
      MARIADB_ROOT_PASSWORD: rootpassword
    volumes:
      - db_data:/var/lib/mysql

  wordpress:
    image: wordpress:6-php8.2-apache
    container_name: lama_wordpress
    depends_on:
      - db
    restart: unless-stopped
    ports:
      - "${WP_PORT:-8080}:80"
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_USER: wordpress
      WORDPRESS_DB_PASSWORD: wordpress
      WORDPRESS_DB_NAME: wordpress
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_HOME', 'https://6f3ba05a533c.ngrok-free.app');
        define('WP_SITEURL', 'https://6f3ba05a533c.ngrok-free.app');
        define('WP_ENVIRONMENT_TYPE', 'development');
        define('WP_DEBUG', true);
        define('GRAPHQL_DEBUG', true);
        define('DISABLE_WP_CRON', false);
        define('WP_MEMORY_LIMIT', '256M');
        define('DISALLOW_FILE_EDIT', true);
        define('GRAPHQL_JWT_AUTH_SECRET_KEY', 'a-long-random-secret');
    volumes:
      - wordpress_data:/var/www/html
      - ./wp-content:/var/www/html/wp-content

  wpcli:
    image: wordpress:cli
    container_name: lama_wp_cli
    depends_on:
      - wordpress
      - db
    working_dir: /var/www/html
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_USER: wordpress
      WORDPRESS_DB_PASSWORD: wordpress
      WORDPRESS_DB_NAME: wordpress
    volumes:
      - ./wp-content:/var/www/html/wp-content
      - wordpress_data:/var/www/html
    entrypoint: ["bash", "-lc"]
    command: |
      "\
      if [ ! -f wp-config.php ]; then \
        echo 'Waiting for WordPress files...'; \
      fi; \
      tail -f /dev/null \
      "

volumes:
  db_data:
  wordpress_data:


